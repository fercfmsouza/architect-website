import Heading from "@/components/Heading";
import Quote from "@/components/Quote";

const AboutPage = () => {
  return (
    <main>
      <Heading />
      <Quote />
    </main>
  );
};

export default AboutPage;
